# mr-timeline

MR 時間軸分析，顯示 MR 從建立到合併的完整時間軸。

## 用途

- 追蹤單一 MR 的完整生命週期
- 顯示所有事件與時間間隔
- 識別 AI Bot vs Human Review
- 分析審查回合數

## 使用方式

```bash
# 分析單一 MR
gitlab-mr mr-timeline 123 -p your-team/your-project

# 分析多個 MR
gitlab-mr mr-timeline 123 124 125 -p your-team/your-project

# 自訂 AI Bot 名稱
gitlab-mr mr-timeline 123 -p your-team/your-project \
  --ai-bots "my-ai-bot,code-reviewer"

# JSON 輸出
gitlab-mr mr-timeline 123 -p your-team/your-project --json

# 詳細模式
gitlab-mr mr-timeline 123 -p your-team/your-project --verbose
```

## 參數說明

| 參數 | 說明 |
|------|------|
| `-p, --project` | GitLab 專案（ID 或路徑） |
| `-t, --token` | GitLab Token（或用環境變數 `GITLAB_TOKEN`） |
| `-u, --url` | GitLab URL（預設 `https://gitlab.com`） |
| `--ai-bots` | 自訂 AI Bot 使用者名稱（逗號分隔） |
| `-j, --json` | JSON 格式輸出 |
| `-v, --verbose` | 詳細輸出 |

## 輸出範例

### 基本輸出（單一 MR）

```bash
gitlab-mr mr-timeline 123 -p your-team/your-project
```

```
═══════════════════════════════════════════════════════════════════════════════
MR #123 時間軸分析
═══════════════════════════════════════════════════════════════════════════════

MR 資訊
────────────────────────────────────────────────────────────────────────────────
  標題: Fix user authentication bug
  作者: developer
  狀態: merged
  週期時間: 1.5 天

時間軸
────────────────────────────────────────────────────────────────────────────────
  11/15 09:30  [MR] MR Created                      developer
               ↓ 2.5h
  11/15 12:00  [C]  Commit Pushed                   developer
               ↓ 1.2h
  11/15 13:15  [AI] AI Code Review                  ai-reviewer
               ↓ 3.0h
  11/15 16:15  [R]  Human Review Started            reviewer1
               ↓ 0.5h
  11/15 16:45  [R]  Review Approved                 reviewer1
               ↓ 0.2h
  11/15 16:55  [M]  MR Merged                       developer

階段摘要
────────────────────────────────────────────────────────────────────────────────
  Dev Time:    2.5h
  Wait Time:   1.2h
  Review Time: 3.7h
  Merge Time:  0.2h
```

### 分析多個 MR

```bash
gitlab-mr mr-timeline 123 124 125 -p your-team/your-project
```

會依序顯示每個 MR 的完整時間軸，並在最後顯示彙總：
```
═══════════════════════════════════════════════════════════════════════════════
彙總統計（3 個 MR）
═══════════════════════════════════════════════════════════════════════════════
  平均週期時間: 1.8 天
  P50 Wait Time: 2.1h
  AI Review 比例: 67% (2/3)
```

### 加上 `--verbose`（詳細模式）

```bash
gitlab-mr mr-timeline 123 -p your-team/your-project --verbose
```

額外顯示：
```
詳細事件資訊
────────────────────────────────────────────────────────────────────────────────
  事件總數: 8
  Commit 數: 3
  Review 留言數: 4

  Review 輪數分析:
    第 1 輪: 提交 → 審查 (2.5h)
    第 2 輪: 修正 → 核准 (1.2h)

  AI vs Human:
    AI Review: 1 次 (ai-reviewer)
    Human Review: 3 次 (reviewer1)
```

### 加上 `--json`（JSON 輸出）

```bash
gitlab-mr mr-timeline 123 -p your-team/your-project --json
```

輸出結構化資料，適合程式處理：
```json
{
  "mr": {
    "iid": 123,
    "title": "Fix user authentication bug",
    "author": "developer",
    "state": "merged"
  },
  "timeline": [
    { "time": "2025-11-15T09:30:00Z", "type": "MR_CREATED", "actor": "developer" },
    { "time": "2025-11-15T12:00:00Z", "type": "COMMIT", "actor": "developer" }
  ],
  "phases": {
    "devTime": 9000,
    "waitTime": 4320,
    "reviewTime": 13320,
    "mergeTime": 720
  }
}
```

### 輸出格式比較

| 參數 | 輸出內容 |
|------|----------|
| （預設） | MR 資訊 + 時間軸 + 階段摘要 |
| 多個 MR | + 彙總統計 |
| `--verbose` | + Review 輪數分析 + AI vs Human |
| `--json` | JSON 格式（適合自動化） |

## 事件類型

| 標記 | 事件類型 |
|------|----------|
| `[MR]` | MR 建立 |
| `[MR📝]` | Draft MR 建立 |
| `[C]` | Commit 推送 |
| `[AI]` | AI Code Review |
| `[R]` | Human Review |
| `[CI]` | CI/CD Bot 回應 |
| `[M]` | MR 合併 |
